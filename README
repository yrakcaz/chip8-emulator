chip8-emulator
==============

This chip8 emulator is obviously able to execute chip8 ROMS.
It can also execute ROMS step by step using -dbg option.

Furthermore, it can disassemble ROMS using --disass option.

It will be able to interpret chip8 asm soon.

For more informations about usage, use:

./chip8-emulator --help
